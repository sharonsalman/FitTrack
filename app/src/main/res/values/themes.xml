<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme with background image -->
    <style name="Base.Theme.FitTrack" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/background</item> <!-- Set your background image here -->
    </style>

    <!-- Application theme inheriting from the base theme -->
    <style name="Theme.FitTrack" parent="Base.Theme.FitTrack">
        <!-- Primary color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@android:color/black</item>

        <!-- Secondary color -->
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@android:color/black</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>

        <!-- Background color (this will be overridden by the image) -->
        <item name="colorOnBackground">@color/colorOnBackground</item>

        <!-- Surface color -->
        <item name="colorSurface">@android:color/black</item>
        <item name="colorOnSurface">@color/colorOnBackground</item>

        <!-- Error color -->
        <item name="colorError">#B00020</item>
        <item name="colorOnError">#FFFFFF</item>
    </style>
</resources>
